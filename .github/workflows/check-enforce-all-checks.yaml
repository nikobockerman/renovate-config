name: Check - Enforce all checks pass
on:
  pull_request:

jobs:
  enforce-all-checks:
    name: Enforce all checks pass
    uses: nikobockerman/github-actions/.github/workflows/check-enforce-all-checks.yaml@v1
    permissions:
      checks: read
