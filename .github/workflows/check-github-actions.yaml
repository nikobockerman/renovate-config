name: Check - Github actions
on:
  pull_request:
    paths:
      - ".github/workflows/**.yaml"
      - ".github/actions/**"
      - "package.json"
      - "yarn.lock"
  workflow_dispatch:

jobs:
  wf:
    uses: nikobockerman/github-workflows/.github/workflows/check-github-actions.yaml@43aca657d9f7a3a58efdf9e2b33668a0f4b6fb6e
