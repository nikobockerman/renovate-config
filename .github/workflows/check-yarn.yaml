name: Check - Yarn and dependencies

on:
  pull_request:
    paths:
      - ".github/workflows/check-yarn.yaml"
      - "package.json"
      - "yarn.lock"
  workflow_dispatch:

jobs:
  wf:
    uses: nikobockerman/github-workflows/.github/workflows/check-yarn.yaml@43aca657d9f7a3a58efdf9e2b33668a0f4b6fb6e
