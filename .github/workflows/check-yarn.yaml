name: Check - Yarn and dependencies
on:
  pull_request:
    paths:
      - ".github/workflows/check-yarn.yaml"
      - "package.json"
      - "yarn.lock"
  workflow_dispatch:
permissions:
  contents: read

jobs:
  wf:
    uses: nikobockerman/github-workflows/.github/workflows/check-yarn.yaml@00f3fa57247684c3937dfdda879ff0f6e95584e4
